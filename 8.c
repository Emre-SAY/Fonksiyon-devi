#include <stdio.h>
void sayi(int sayi){
    int sayi;
    printf("Bir sayı giriniz:");
    scanf("%d", &sayi);
    if(sayi>0)
    printf("sayi pozitiftir.");
    if(sayi<0)
    printf("sayi negatiftir.");
    if(sayi=0)
    printf("sayi sifirdir.");
}
int main(){
    sayi;
}